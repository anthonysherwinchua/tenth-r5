- content_for :page_header do
  | Contact Types

- content_for :breadcrumbs do
  li = link_to 'Lookups', admin_lookups_path
  li = link_to 'Contact Types', admin_lookups_contact_types_path

.row
  .col-xs-12

    .box
      .box-header
        .box-title

        .box-tools
          .no-margin.pull-right
            = link_to 'Add new', new_admin_lookups_contact_type_path, class: 'btn btn-primary', data: { toggle: 'modal', target: '.modal' }

      .box-body.no-padding
        table.table.table-hover.table-striped
          thead
            tr
              th Contact Type
              th
          tbody
            - @contact_types.each do |contact_type|
              tr
                td = contact_type.name
                td = link_to 'Edit', edit_admin_lookups_contact_type_path(contact_type), class: 'btn btn-default', data: { toggle: 'modal', target: '.modal' }
