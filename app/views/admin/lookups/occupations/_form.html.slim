.box.box-primary
  .modal-header.box-header.with-border

    button.close aria-label="Close" data-dismiss="modal" type="button"
      span aria-hidden="true"  ×

    h4.modal-title.box-title Occupation Details

  .modal-body.box-body
    .row
      .col-xs-12
        = f.input :name, label: 'Occupation'
        = f.input :shorthand, label: 'Shorthand'
        = f.input :enabled, label: 'Enabled?', disabled: !policy(f.object).disable?

  .modal-footer.box-footer
    = f.submit 'Save', class: 'btn btn-flat btn-primary btn-block'
