- content_for :page_header do
  | Skin Types

- content_for :breadcrumbs do
  li = link_to 'Lookups', admin_lookups_path
  li = link_to 'Skin Types', admin_lookups_skin_types_path

.row
  .col-xs-12

    .box
      .box-header
        .box-title

        .box-tools
          .no-margin.pull-right
            = link_to 'Add new', new_admin_lookups_skin_type_path, class: 'btn btn-primary', data: { toggle: 'modal', target: '.modal' }

      .box-body.no-padding
        table.table.table-hover.table-striped
          thead
            tr
              th Skin Type
              th
          tbody
            - @skin_types.each do |skin_type|
              tr
                td = skin_type.name
                td = link_to 'Edit', edit_admin_lookups_skin_type_path(skin_type), class: 'btn btn-default', data: { toggle: 'modal', target: '.modal' }
